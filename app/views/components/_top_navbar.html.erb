<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navlink" do %>
    <%= image_tag("map.png") %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Accueil", root_path, class: "nav-link" %>
      </li>
      <li class="nav-item dropdown">
        <% if (current_user != nil) && (current_user.avatar == nil) %>
          <%= '<i class="fas fa-user avatar dropdown-toggle" id: "navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>'.html_safe %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' } %>        <% elsif (current_user != nil) && (current_user.avatar != nil) %>
          <%= '<i class="fas fa-user"></i>'.html_safe %>
        <% else %>
          <%= image_tag "logo.svg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Se connecter", new_user_session_path, class: "dropdown-item" %>
            <%= link_to "S'inscrire", new_user_registration_path, class: "dropdown-item" %>
          </div>
        <% end %>
      </li>
    </ul>
  </div>
</div>
