<h1>Modifier le lieu-dit <%= @lieudit.name %> (<%= @ville.name %>)</h1>
<%= simple_form_for [@lieudit] do |f| %>
  <%= f.input :name, label: "Nom" %>
  <%= f.button :submit, "Modifier" %>
<% end %>
<%= link_to "Retour", ville_path(@ville) %>
