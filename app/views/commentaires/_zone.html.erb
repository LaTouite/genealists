<%= simple_form_for([@acte, @commentaire], method: :post, url: acte_commentaires_path(@acte), remote: true) do |f| %>
  <%= f.input :contenu, label: "Commentaire"  %>
  <%= f.button :submit, "Ajouter" %>
<% end %>
